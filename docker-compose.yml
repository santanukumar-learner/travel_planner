
services:
  pathway:
    build: .
    container_name: pathway-vectorstore
    working_dir: /app
    volumes:
      - ./:/app
    command: ["python", "/app/pathway_service.py"]
    ports:
      - "8765:8765"
    environment:
      PYTHONUNBUFFERED: "1"

